public enum StarType {
    O, B, A, F, G, K, M, NEUTRON, BH
}

public enum StarSize {
    DWARF, SMALL, MEDIUM, LARGE, GIANT, SUPER_GIANT
}

public enum PlanetSize {
    ROCK_SMALL, ROCK_MEDIUM, ROCK_LARGE,
    GAS_SMALL, GAS_MEDIUM, GAS_LARGE
}

public enum MoonSize {
    SMALL, MEDIUM, LARGE
}

public enum Environment {
    ROCKY, ARID, DESSERT, FORREST, JUNGLE, WATER, ICE, VACUUM, VOLCANIC
}

public enum ShuttleStatus {
    MISSING, BROKEN, STANDBY, ON_MISSION
}

public enum WeaponType {
    CHARGE_SHOT, ROCKET, EMP
}